import { SubjectRegistration } from './subject-registration.model';

describe('SubjectRegistration', () => {
  it('should create an instance', () => {
    expect(new SubjectRegistration()).toBeTruthy();
  });
});
